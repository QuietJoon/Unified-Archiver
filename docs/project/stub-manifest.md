---
type: Stub Manifest
title: "Stub Manifest (Retrospective)"
description: "This is a retrospective record."
tags: [project-control, OI-0057-003, OI-0065-002]
timestamp: 2026-04-13T00:00:00Z
status: active
---

# Stub Manifest (Retrospective)

> This is a retrospective record. Instead of planned STUB markers, this documents remaining incomplete features as DEFERRED entries.

- **Manifest version:** 1
- **Date:** 2026-04-13
- **Notes:** Implementation is complete for in-scope MVP scenarios with tracked gaps. These entries represent explicitly out-of-scope features that are architecturally present but not yet functional.

| ID | Marker | Scope | Path | Binary / Module | Scenario / Slice | Contract / Interface | Dummy Behavior | Exit Condition | Status |
|---|---|---|---|---|---|---|---|---|---|
| DEF-001 | ~~DEFERRED~~ | Future | `src/archive.rs`, `src/ffi/common.rs`, all backend wrappers | archive | SCN-SFX-* (SFX open flow) | `Archive::open_at_offset` | `Archive::open_at_offset(path, offset)` works for all backends — ZIP (via `OffsetReader`) and 7z/TAR/ISO/RAR (via tempfile slice). | — | **Closed 2026-04-18** (Workstream C of pre-v0.1 gap closure) |
| DEF-002 | DEFERRED | Future | `src/options.rs` | options | SCN-CRE-* (split creation) | `CompressionOptions::split_size` | Field exists but ignored by all writers | At least one writer honors split_size | Open |
| ~~DEF-003~~ | ~~DEFERRED~~ | Future | `src/options.rs`, backends | options, archive, ffi/* | All password scenarios | Password fields (String -> SecStr) | `ExtractionOptions.password` and `CompressionOptions.password` are `Option<SecStr>`; UnRAR/WinRAR wrappers intern passwords as `SecStr` and decode only at the FFI boundary. | All password fields use SecStr or equivalent | **Closed 2026-04-17** (OI-0057-003) |
| DEF-004 | DEFERRED | Future | `src/ffi/wrapper.rs`, `src/ffi/sevenz_wrapper.rs`, `src/ffi/zip_wrapper.rs`, `src/streaming.rs` | ffi/*, streaming | SCN-EXT-* (streaming) | `extract_to_stream` | All three non-libarchive backends (ZIP, SevenZ, UnRAR) internally call `extract_to_memory()` and wrap the buffer in `Cursor`, so memory use is O(entry size) rather than O(window). Only libarchive actually streams. The streaming contract documents this caveat for callers. **Narrowed 2026-08-12 (R0001-0011 / DCR-006 Amendment 3):** the first step landed — the backend materialisation budget is now derived from `StreamBound` (`min(bound, max_file_size, max_total_size)`) and ZIP/7z gained `extract_to_stream_with_limit` overrides, so the buffer is bounded by the caller's chosen bound and an over-budget entry is refused before it is materialised. What remains deferred is true incremental (non-buffering) streaming for these three backends. | All three backends provide true streaming readers (no full buffer) via `StreamingExtractor` ownership of backend handle lifetime | Open |
| DEF-005 | ~~DEFERRED~~ **Accepted 2026-09-03** | — | `src/modification.rs`, `src/ffi/zip_writer.rs`, tests | modification | SCN-MOD-* (ZIP modify) | `commit_changes` for ZIP | 2026-04-18: archive-level ZIP comment and per-entry compression method now round-trip through `commit_changes` (Workstream D). Remaining deferrals (documented in `Limitations.md` §2, "Modification is rewrite-based and limited to ZIP / 7z"): ZIP64 >4 GiB boundary coverage, encrypted-ZIP re-encryption (rejected), and crash-recovery journaling. | All remaining caveats closed or formally accepted | **Closed (2026-09-03, owner ruling)** — the three remaining caveats are now *decided* rather than pending, which is what this deferral's own exit criterion asked for. Encrypted-ZIP re-encryption was already REJECTED. Crash-recovery journaling is settled by the design: commit is tempfile + atomic rename, and `Limitations.md` §2 states there is no journal. ZIP64 above 4 GiB is ACCEPTED AS A PERMANENT CAVEAT rather than covered: the write path exists but is untested at the boundary, and multi-GiB fixtures are slow, awkward to keep in a suite, and would have to live outside the repository. The caveat is already public in `Limitations.md` §2, so a caller is not misled. With nothing undecided left, the marker is struck. See also the 2026-09-03 ruling that libarchive stays the permanent ZIP rewriter — these two were often read as one question and are not. |
| DEF-006 | ~~DEFERRED~~ | Future | `src/creation.rs`, `src/ffi/zip_writer.rs`, `src/ffi/libarchive_wrapper.rs` | creation | SCN-CRE-04 (creation progress) | `CompressionOptions::progress` | Both ZIP (zip-rs) and libarchive backends invoke the callback per-entry; `ControlFlow::Break` surfaces as `ArchiveError::format(_, "Creation cancelled by user")` | At least one creation backend invokes progress callbacks | **Closed (2026-04-13, OI-025-003 resolved for wiring portion)** |
| DEF-007 | ~~DEFERRED~~ | Future | `src/sfx/detection.rs`, `src/sfx/stub_types.rs` | sfx/detection | SCN-SFX-08 (unknown stub) | `StubType::Unknown` | `StubType::detect()` returns `StubType::Unknown` for unrecognized executables (infallible — it returns a variant, not a `Result`). **Superseded 2026-08-16 (R0070-0076):** an `Unknown` stub is now *rejected* at Stage 1 and `detect_sfx` returns `not_sfx()` before the Stage-2 signature scan. See SCN-SFX-08 in `docs/architecture/scenario-matrix.md` and `docs/records/AD-0060-r0070-broad-modular-triage-closure.md`. | Heuristic scanning identifies archives in unknown stub types (criterion withdrawn by R0070-0076) | **Closed (2026-04-13, OI-027-001 resolved)** |
| DEF-008 | ~~DEFERRED~~ | Future | `src/modification.rs` | modification | SCN-MOD-* (modification options) | `ModificationOptions` | `Archive::modify_with_options(path, opts)` honors all fields: `create_backup` + `backup_suffix` (sidecar copy), `preserve_metadata` (modified, accessed, and created timestamps plus Unix permissions on retained regular-file entries via metadata-aware add helpers — OI-0065-002 resolved; directory metadata still uses backend defaults), `compression` (override settings). | All `ModificationOptions` fields honored end-to-end | **Closed (2026-04-14, OI-025-001/OI-025-002 resolved)** |
