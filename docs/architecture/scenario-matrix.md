# Scenario Matrix

Scenario-to-system mapping for all mandatory MVP scenarios.

## Inspection Scenarios

| ID | Scenario | Actor | Trigger/Input | Expected Output | Modules Touched | Persistence | Files Touched | Integrations | Core Verification? | Critical Negative Path? |
|---|---|---|---|---|---|---|---|---|---|---|
| SCN-INS-01 | Inspect archive contents (unified API) | Rust developer | Archive file (ZIP, RAR/RAR5, 7z, TAR, TAR.GZ, TAR.BZ2, TAR.XZ). Note: standalone gzip/bzip2/xz only supported as TAR compound formats. | &[ArchiveEntry] (cached slice) with path, size, compressed_size, modified, crc32 (selected fields shown; see ArchiveEntry for full definition) | archive, inspection, format, entry, ffi/* | None | Input archive (read) | UnRAR or libarchive or piz or sevenz | Yes | No |
| SCN-INS-02 | Inspect password-protected metadata (format-dependent) | Rust developer | Encrypted archive, no password | ZIP/RAR (standard encryption): filenames + metadata accessible without password; content protected. RAR5 with header encryption: metadata listing requires the password (returns `ArchiveError::Password`). 7z: header encryption may also block metadata listing. Behavior is format- and encryption-mode-specific, not a universal guarantee. | archive, inspection, format, entry, ffi/unrar_wrapper, ffi/zip_wrapper, ffi/sevenz_wrapper | None | Input archive (read) | UnRAR (RAR), zip crate (ZIP), SevenZ (7z) | Yes | Yes (ArchiveError::Password if content accessed or header-encrypted) |
| SCN-INS-03 | Validate archive integrity via CRC32 | Rust developer | Archive file | Corruption detected per-file or Ok | archive, inspection, security, ffi/* | None | Input archive (read) | UnRAR, libarchive (auto-CRC during extract), Piz (ZIP), SevenZ (7z) | Yes | Yes (ArchiveError::Corruption) |
| SCN-INS-04 | Inspect multi-part archive | Rust developer | First part of split archive (.part1.rar) | Complete file list from all parts. Note: ZIP split archives (.z01) are unsupported. | archive, inspection, ffi/wrapper | None | All parts (read) | UnRAR (auto-handles parts) | No | No |
| SCN-INS-05 | Filter large archive listings | Rust developer | Archive with 1000+ files + filter predicate | Filtered entries with metadata. Note: the inspection contract does not define a dedicated filter API; filtering is performed caller-side over the returned `&[ArchiveEntry]` slice. | archive, inspection, entry | None | Input archive (read) | Backend (any) | No | No |

## Extraction Scenarios

| ID | Scenario | Actor | Trigger/Input | Expected Output | Modules Touched | Persistence | Files Touched | Integrations | Core Verification? | Critical Negative Path? |
|---|---|---|---|---|---|---|---|---|---|---|
| SCN-EXT-01 | Extract all files (unified API) | Rust developer | Archive + ExtractionOptions (destination) | All files extracted with correct structure/content | archive, extraction, security, options, ffi/* | None | Input archive (read), extracted files (write) | Backend (any) | Yes | No |
| SCN-EXT-02 | Extract password-protected archive | Rust developer | Encrypted archive + correct password | Decrypted files extracted | archive, extraction, security, options, ffi/unrar_wrapper, ffi/zip_wrapper, ffi/sevenz_wrapper | None | Input archive (read), extracted files (write) | UnRAR (RAR), zip crate (ZIP), SevenZ (7z) | Yes | Yes (ArchiveError::Password on wrong password) |
| SCN-EXT-03 | Extract multi-layer compressed | Rust developer | TAR.GZ / TAR.BZ2 / TAR.XZ archive | Transparent multi-layer decompression | archive, extraction, format, ffi/libarchive_wrapper | None | Input archive (read), extracted files (write) | libarchive | Yes | No |
| SCN-EXT-04 | Handle corrupted archive | Rust developer | Invalid/corrupted archive | ArchiveError::Corruption or format-specific error on clear corruption; not all damage is detectable. | archive, extraction, ffi/*, error | None | Input archive (read) | Backend (any) | Yes | Yes (must produce clear error on detectable corruption) |
| SCN-EXT-05 | Monitor extraction progress | Rust developer | Large archive + ProgressCallback | Rate-limited progress updates (frequency varies by backend) | archive, extraction, options (RateLimiter) | None | Input archive (read), extracted files (write) | Backend (any) | No | No |

## Creation Scenarios

| ID | Scenario | Actor | Trigger/Input | Expected Output | Modules Touched | Persistence | Files Touched | Integrations | Core Verification? | Critical Negative Path? |
|---|---|---|---|---|---|---|---|---|---|---|
| SCN-CRE-01 | Create archive in multiple formats | Rust developer | Files + CompressionOptions (format) | Valid archive readable by standard tools. Note: 7z creation uses libarchive, while 7z read uses the native SevenZ backend (different backend families). | archive, creation, format, options, ffi/libarchive_wrapper, ffi/zip_writer | None | Source files (read), new archive (write) | libarchive (7z creation, TAR), zip crate (ZIP) | Yes | No |
| SCN-CRE-02 | Create with max compression | Rust developer | Files + Maximum compression level | Optimal compression per format | archive, creation, options, ffi/* | None | Source files (read), new archive (write) | libarchive or zip crate | No | No |
| SCN-CRE-03 | Create password-protected archive (ZIP-only) | Rust developer | Files + password | Encrypted ZIP archive. Note: creation-time encryption is currently ZIP-only (via zip crate AES-256). 7z and TAR encrypted creation is not supported and is future work. | archive, creation, options, ffi/zip_writer | None | Source files (read), encrypted archive (write) | zip crate (ZIP encryption) | No | No |
| SCN-CRE-04 | Create from large dataset with progress | Rust developer | 10GB+ data + ProgressCallback | Per-entry progress invoked during creation with `total=None` (OI-025-003 resolved, AD 0021); bounded memory unverified for creation path | archive, creation, options | None | Source files (read), new archive (write) | Backend (any) | No | No |
| SCN-CRE-05 | Create with compression level control | Rust developer | Pre-compressed files + level adjustment | Adjustable compression overhead | archive, creation, options | None | Source files (read), new archive (write) | Backend (any) | No | No |

## Modification Scenarios

| ID | Scenario | Actor | Trigger/Input | Expected Output | Modules Touched | Persistence | Files Touched | Integrations | Core Verification? | Critical Negative Path? |
|---|---|---|---|---|---|---|---|---|---|---|
| SCN-MOD-01 | Add files to existing archive | Rust developer | Archive + new files | Archive contains original + new entries. Metadata (timestamps, permissions) preserved when `preserve_metadata` is enabled. Compression configurable via `ModificationOptions.compression`. | archive, modification, creation, ffi/libarchive_wrapper | On-disk (temp file + atomic rename) | Source archive (read), temp archive (write), atomic rename | libarchive (read), creation backends (write) | Yes | No |
| SCN-MOD-02 | Remove entries from archive | Rust developer | Archive + entry names to remove | Entries removed, archive size reduced. Metadata and compression settings preserved per `ModificationOptions`. | archive, modification, creation | On-disk (temp file + atomic rename) | Source archive (read), temp archive (write), atomic rename | libarchive (read), creation backends (write) | Yes | No |
| SCN-MOD-03 | Replace file in archive | Rust developer | Archive + updated file | Logical replacement via copy-on-write archive rewrite. Metadata and compression settings preserved per `ModificationOptions`. | archive, modification, creation | On-disk (temp file + atomic rename) | Source archive (read), temp archive (write), atomic rename | libarchive (read), creation backends (write) | No | No |
| SCN-MOD-04 | Modify large archive efficiently | Rust developer | Large archive + modification | Temp disk scales with archive size (full rewrite). Metadata and compression settings preserved per `ModificationOptions`. | archive, modification, creation | On-disk (temp file + atomic rename) | Source archive (read), temp archive (write) | libarchive, creation backends | No | No |

## SFX Detection Scenarios

> **Note:** No confirmed (1.0 confidence) detection path currently exists in the implementation. All detections return `probable()` (~0.9). The `SfxConfidence` result model retains `probable`/`confirmed` semantics for future use when higher-confidence heuristics are added.

| ID | Scenario | Actor | Trigger/Input | Expected Output | Modules Touched | Persistence | Files Touched | Integrations | Core Verification? | Critical Negative Path? |
|---|---|---|---|---|---|---|---|---|---|---|
| SCN-SFX-01 | Detect Windows PE SFX (ZIP) | Rust developer | Windows .exe with embedded ZIP | is_sfx=true, format=ZIP, offset, stub_type=WindowsPE. Detection: Stage 1 (4KB header read for stub-type identification) then Stage 2 (up to 1MB signature scan for archive offset). `probable()` returns ~0.9 (detector policy); no 1.0 confirmed path currently. | sfx/detection, sfx/signatures, sfx/stub_types, format | None | Executable file (read, first 4KB + up to 1MB) | goblin (PE parsing) | Yes | No |
| SCN-SFX-02 | Detect WinRAR SFX | Rust developer | WinRAR .exe | is_sfx=true, format=RAR/RAR5, offset, stub_type=WindowsPE. Detection: Stage 1 (4KB) + Stage 2 (1MB). `probable()` ~0.9. | sfx/detection, sfx/signatures, sfx/stub_types | None | Executable file (read, first 4KB + up to 1MB) | goblin | Yes | No |
| SCN-SFX-03 | Detect 7-Zip SFX | Rust developer | 7-Zip .exe | is_sfx=true, format=7z, offset, stub_type=WindowsPE. Detection: Stage 1 (4KB) + Stage 2 (1MB). `probable()` ~0.9. | sfx/detection, sfx/signatures, sfx/stub_types | None | Executable file (read, first 4KB + up to 1MB) | goblin | Yes | No |
| SCN-SFX-04 | Detect Linux ELF SFX | Rust developer | ELF binary with embedded archive | is_sfx=true, correct format, offset, stub_type=LinuxELF. Detection: Stage 1 (4KB) + Stage 2 (1MB). `probable()` ~0.9. | sfx/detection, sfx/signatures, sfx/stub_types | None | Executable file (read, first 4KB + up to 1MB) | goblin (ELF parsing) | Yes | No |
| SCN-SFX-05 | Detect ScriptInterpreter SFX | Rust developer | Script interpreter + embedded archive (ZIP, RAR/RAR5, 7z) | is_sfx=true, format detected, offset, stub_type=ScriptInterpreter. Detection: Stage 1 (4KB shebang check) + Stage 2 (1MB signature scan). `probable()` ~0.9. Active signature set: ZIP, RAR/RAR5, 7z (gzip/bzip2/xz excluded). | sfx/detection, sfx/signatures, sfx/stub_types | None | Script file (read, first 4KB + up to 1MB) | (shebang detection, no binary parser needed) | Yes | No |
| SCN-SFX-06 | Reject standard archive (not SFX) | Rust developer | Regular archive file (ZIP, RAR, 7z) | is_sfx=false, stub_type identification fails at Stage 1 | sfx/detection, sfx/stub_types | None | Archive file (read, first 4KB header) | goblin (fails PE/ELF parse) | Yes | No |
| SCN-SFX-07 | Reject non-archive executable | Rust developer | Regular binary (no embedded archive) | is_sfx=false, Stage 2 signature scan finds no archive magic | sfx/detection, sfx/signatures, sfx/stub_types | None | Executable file (read, first 4KB + up to 1MB) | goblin | Yes | No |
| SCN-SFX-08 | Detect SFX with unknown stub | Rust developer | SFX with custom stub | Resolved (OI-027-001): unknown stubs classified as `StubType::Unknown` and proceed to signature scanning; returns `probable()` with confidence 0.9 when archive signature found. Tested with synthetic coverage. | sfx/detection, sfx/signatures, sfx/stub_types | None | File (read, first 4KB + up to 1MB) | goblin | Yes | No |
