# Documentation Log

## 2026-08-09 — Review 0001 decision gate

* **Update**: Ran `/indy-review-gate` over `reviews/0001.md` — a fresh independent review filed the same day carrying **95 findings (1 Critical, 32 High, 49 Medium, 13 Low)** plus a hand-authored, unapplied patch covering ~14 ids across 9 files. Integrity checked out (claim token matched in both files, both status lines `COMPLETED`, 95 unique ids). Roughly 70 findings were verified directly against the tree before triage and **every one checked was accurate**, including the non-obvious ones: `record_dir` really did skip the ancestor loop `record_file` runs, so a file `a` plus a directory `a/b` was accepted in either order; `goblin` really is in neither `Cargo.toml` nor `Cargo.lock` despite eight architecture rows crediting it; and the `FileAttributes` table had 7z and RAR exactly reversed for `archive_specific` (`wrapper.rs` sets `Some`, `sevenz_wrapper.rs` sets `None`). Triage: **77 ACCEPT, 1 REJECT, 9 DEFER, 8 escalation candidates**. The single reject is R0001-0058 (RAR progress advancing by declared rather than decoded bytes) — deliberate, because `total_bytes` is built from declared sizes and a decoded-byte numerator would reintroduce exactly the basis mismatch R0080-0065 fixed on the libarchive side.
* **Update**: The owner routed all 17 Phase 2 items as the gate suggested — **4 fix, 2 ADR, 11 track**. Two of them collided with prior decisions and were surfaced before the routing, not after: R0001-0007 (streaming never sends the per-entry cap to the backend) is *already* open TicGit `6277386e`, filed by the owner on 2026-08-05 and triaged as "CONFIRMED … excluded and still open"; and R0001-0050 (the content-multiset digest depends on duplicate-path grouping) contradicts ticket `2a6e3153`, **closed as resolved** on 2026-07-19, whose stated goal was to keep the per-path occurrence ordinal. That closure fixed a different problem — duplicate-path CRC-less archives could not be digested at all — and never examined the *keying*, which is what makes the digest layout-dependent against its own rustdoc and AD-0047. `OI-0001-008` records the contradiction verbatim so whoever picks it up knows they are overturning a deliberate decision rather than fixing an oversight.
* **Repair**: **81 fixes landed** (77 accepts + the 4 Phase-2 `fix` routes), applied by 16 file-scoped agents — one owner per file, so no two could touch the same source — then compiled, linted and tested centrally. Outcome: **79 fixed, 2 partial, 1 blocked-then-completed-by-hand**, with exactly **one** compile error across the whole batch. Security-relevant highlights: the Critical R0001-0001 — only symlinks and hard links were filtered before `archive_write_header`, so a crafted archive could have `archive_write_disk` materialise FIFOs, sockets and device nodes inside the destination, although `parse_entry` already classified them `EntryType::Other`; R0001-0012 — `Archive::open` probed a header **only** for compressed-tar extensions, so AD-0052's eager-validation carve-out, the one backend documented to fail fast, did not; R0001-0028/0029 — the ZIP unattributed-duplicate guard was `raw > deduped && names.is_empty()`, so a single attributable duplicate disarmed it, and lossy UTF-8 tallying recorded keys the crate's decoded lookup can never match; R0001-0015/0016 — the disk `copy_data` path lacked the null-buffer and backward-block guards its memory sibling already had. Also landed: exact-size enforcement on ZIP memory extraction and three integrity walks, `min(max_file_size, max_total_size)` as one effective cap across four entry points, ancestor reservation for directories, mode-gate-before-path-validation ordering, ELF and Mach-O fixed-header validation, `_is_set`-based size extraction (unknown sizes are now honestly `None`, not `Some(0)`), nanosecond timestamps, and pre-1970 7z timestamps.
* **Repair**: Corrections the subagents made **to the gate's own guidance**, each verified and each right. On R0001-0029 the gate claimed the duplicate-accounting fix alone closed the escape hatch; it does not, because two byte-identical non-UTF-8 records lossy-decode to the *same* key and are therefore fully attributed — attribution now uses raw name bytes joined through `ZipFile::name_raw()`. On R0001-0033 the review said an oversized comment is serialised with a truncated `u16` length; `zip` 2.4.2 actually rejects it inside `Zip32CentralDirectoryEnd::write`, so the real symptom was a **late** failure at `finish()` after every entry was already compressed — which, combined with R0001-0018's new sticky terminal state, would have made the archive unrecoverable at the last possible moment. On R0001-0024 the exposure was wider than stated: upstream compares the CRC only in its `remaining <= 0` branch, so a short 7z entry passed even *with* a valid kCRC digest. On R0001-0080 the doc had attributed a real removal to the wrong signatures — AD-0015 removed the ZIP central-directory signature and TAR's `ustar`, not gzip/bzip2/xz. And R0001-0088's premise that `test_recovery.rar` carries a recovery record is false: it is byte-identical to `test.rar` and `test_rar5.rar` (all three 97 bytes, same SHA-256) and its RAR5 archive-flags vint is `0`.
* **Repair**: Four fixes the gate completed centrally because file ownership or cross-cutting scope put them out of every agent's reach. R0001-0003 was correctly refused by its agent rather than bodged — `revalidate_locked_identity` was module-private and `LockedFileIdentity`'s inner field is private, so expressing the check locally would have meant duplicating the helper and its DCR-007 wording; it is now `pub(crate)` and brackets the transient 7z probe in `Archive::is_solid` on both sides. The synthetic ELF fixture in `src/sfx/detection.rs` had to gain `e_type`/`e_version`/`e_ehsize` or R0001-0066's stricter `is_elf` would have failed it. `Archive::finish` now marks the handle finalized on the **failure** path too, because R0001-0018's sticky terminal state otherwise made `Drop` re-report an error the caller already held, advising them to call `finish()` — which they just had. And the `Corruption` Display change propagated to `specs/001-unified-archive/contracts/errors.md` and the user manual, which quoted the old template.
* **Update**: **Two decision records and one design-change record.** `MADR-0031` disposes of R0001-0035 (tiny files trust any extension mapping): rejected, because the sub-four-byte branch and the post-magic fallback answer different questions — "no evidence" is not "contrary evidence" — and gating it would trade a format-specific diagnostic for a bare size complaint on exactly the truncated inputs that reach it. `MADR-0032` disposes of R0001-0041 (selective extraction divides by the whole archive): the ratio is **informational** for subsets and the decoded-byte caps bind, because a format-specific accounting pass is impossible precisely where the risk lives — the compressed-TAR family exposes no per-entry compressed size. `DCR-010` records that extraction now rejects non-regular, non-directory entries on every backend, paired with an amendment to `MADR-0010`. That amendment is the honest part: MADR-0010's own driver is "callers need to know what was skipped — silent data loss is unacceptable", and the new skip class is **silent**, because no `ArchiveWarning` variant fits a device node and reusing `SkippedSymlink` would assert something false to the caller most likely to be auditing. `AD-0052` and `DCR-006` also gained amendments — the latter recording that `StreamBound::Unbounded` no longer means "no cap anywhere", since the backend read is now bounded by `max_file_size` regardless of the bound chosen.
* **Update**: Eleven findings were routed to `track` and are now registered in both places: **ten new OI entries** (`OI-0001-001` … `OI-0001-010`, consolidating R0001-0026 + R0001-0030 since one raw central-directory index fixes both) and ten matching `docs/backlog.md` entries — one Type 1, six Type 2, three Type 3. Nothing was deleted or reordered and no `### Notes` block was touched. Two OI entries carry warnings a future reader needs: `OI-0001-006` should be **absorbed into OI-0080-004** rather than resolved on its own, because that issue replaces the string-matching it patches; and `OI-0001-008` carries the closed ticket's reasoning verbatim.
* **Verification**: `cargo fmt --all` 0, `cargo check --all-features --all-targets` 0, `cargo clippy --all-features --all-targets` 0 with **zero Rust warnings**, and `TMPDIR=/Volumes/Temp/claude cargo test --workspace --all-features -- --test-threads=4` green — **31 suites, 1460 passed, 0 failed, 3 ignored**, up from 1233 before the batch, so the fixes added roughly 227 tests. Record-status audit 0 problems across 148 `index.yaml` entries; 266 docs, 0 dangling relative links. One caveat worth recording rather than burying: the first suite run failed a single test on `ENOSPC` — `/Volumes/Temp` was at 100% and `test_validation_large_file_stress` could not write its 5 MB fixture. It aborts at `fs::write` before any library call, so it was never a code defect; the owner freed space and the re-run was clean. (It also incidentally proved R0001-0086: the lane *ran* rather than skipping, so the new PATH search finds `zip` where the old `which` shell-out would have.) `src/ffi/common.rs`'s Windows NUL rejection (R0001-0073) is the one change **not** verified by this run — it is `cfg(windows)` and cannot compile on this host, so it is syntactically validated only and needs the Windows CI job, same posture as OI-0081-005.
* **Update**: Six follow-up tickets filed for what the batch surfaced but could not close, one of which is a genuine new defect the review never found: **RAR5 stores the Unix mode in the file-header Attributes vint unshifted**, while `src/ffi/wrapper.rs` applies the RAR4 `>> 16` unpacking — so for `tests/fixtures/test.rar` the vint decodes to `33188` (`0o100644`) and the entry reports `permissions: Some(0)`, a positive claim of mode 0000, which is strictly worse than `None`. That is what holds R0001-0093's RAR lane at presence-only. The others: the missing `ArchiveWarning` variant above; the libarchive I/O-vs-corruption classifier that R0001-0056 needs and that no existing helper can express without an `archive_errno` declaration; the RAR fixture gaps (no archive in the repository carries a recovery record, and none is both encrypted and recovery-bearing); documentation residue outside the batch's file ownership; and FFI declaration hygiene, since the `_nsec` externs had to be declared in a third file.

## 2026-08-07 (2) — review-archive cleanup

* **Update**: Ran `/indy-review-cleanup` over `reviews/reviewed/` and retired the whole archive. Every finding that no record, open issue, or source comment cited was re-assessed against the current tree — **1,218 findings across 19 reviews (0056–0081)**, verdicted per finding by 29 parallel assessors, each writing its result durably to disk. Outcome: **864 FIXED** (the code already does what the finding asked), **272 COVERED_BROADER** (a record or OI covers the concern without citing the id), **73 DEAD** (premise gone — most often the `piz` backend removed by DCR-009), and **9 LIVE** (0.7%). The 19 `.md` files and their 9 paired `.patch` proposals were **moved, not deleted**, to `/Volumes/Common/QJoon/Unified-Archiver/reviews-cold-archive-2026-08-07/` with a README explaining the triage: `reviews/` is gitignored and was never tracked, so deletion would have been irreversible with no git history to recover from. Finding ids stay stable and still resolve through `docs/records/index.yaml`; the cold store is where to look up what an id originally said.
* **Update**: The nine live findings were routed before the move. Four new open issues: **OI-0080-006** (libarchive's header-status policy is split across six read walks — `test_integrity` and both stream walks accept `ARCHIVE_WARN` while `list_files`/`extract_all`/`extract_file` reject it, so one archive can be simultaneously "clean" and "broken", and `ffi-libarchive.md` documents a uniform contract only three sites implement); **OI-0080-007** (7z backend: corruption on an encrypted entry is reclassified as `Password` with no way for a caller to tell damaged media from a wrong passphrase, and `test_integrity`'s solid-stream drain swallows the first error so a later good file can be blamed — 7z is the outlier, the sibling findings were fixed the opposite way in libarchive and UnRAR); **OI-0080-008** (a standards-conforming `.zst` with a leading skippable frame cannot be opened by any path — RFC 8878 permits the frame, detection does not, and `Zst` is absent from the extension-fallback set); **OI-0056-010** (two test lanes that silently self-disable: fixtures built via the external `zip` CLI that skip when it is absent, and a perf baseline that prints a verdict instead of asserting it). Each carries a TicGit ticket (`12d4310b`, `a17daf34`, `75db9f0f`, `37182812`).
* **Repair**: Three live findings were one-line defects and were fixed inline rather than filed. `src/sfx/result.rs` linked the `#[cfg(test)]` `SfxDetectionResult::detected` constructor from public rustdoc — a real `cargo doc` warning, now gone (verified: the crate's only remaining lib doc warning is the unrelated `Display` link at `src/password.rs`, ticket `c7492400`). The crate-root example in `src/lib.rs` and `examples/detect_sfx.rs` both taught the `unwrap()`-each-accessor pattern that `payload_coordinates()` was added to retire; both now use the joined accessor. And `tests/modification_options_test.rs`'s module header pointed readers at a file with zero metadata assertions while claiming `created`/`accessed` are not round-tripped — both false since April, contradicted by a test in the same file.
* **Repair**: Side defects surfaced by the assessment. Four **mis-cited `Source:` lines** in the OI-0057-* entries of `open-issues-resolved.md`: OI-0057-002 (RAR feature flag) cited the path-sanitization and `modify()` findings, OI-0057-006 (non-atomic writes) cited two UnRAR *build* findings, OI-0057-007 (`commit_changes` buffering) cited the Windows-build and Homebrew-path findings, and OI-0057-005 cited two whole-entry-buffering findings alongside its correct one — all corrected to the ids the entries actually track, each with a dated note naming what the old ids really were. **AD-0067** gained an amendment recording that R0076-0051 was never in its routing table (harmless — the tree fixed it under R0080-0063). **DCR-003** gained an amendment transcribing `0078.patch`'s `Issue mapping` header verbatim: Review 0078 never got a closure record, and that header was the sole disposition trail for ~80 of its findings. README and `design-baseline.md` both still described TAR.ZST/TAR.LZ4/TAR.LZMA creation as deferred to v0.4, contradicting `ArchiveFormat::can_create` — corrected in the opposite direction from the usual drift.
* **Verification**: Re-ran `/indy-review-prune` after the cleanup and it came back a **no-op** — recorded here so a future run need not repeat the classification. Its hard gate (has cleanup run?) passed without a question: `reviews/reviewed/` is empty because the cleanup emptied it minutes earlier. The store delta since the morning's prune is +4 OPEN open-issues written by that cleanup (code paths re-verified present, so RETAIN by rule) and two amendments to already-retained records; no record was added or removed, and nothing in the tree invalidated an earlier verdict. Final state: 97 live records (79 active / 13 superseded / 5 archived) + 48 legacy DD/IG, and 35 OI entries (14 OPEN, 4 partial, 17 resolved) — all retained. One deliberate non-action: by the strict "fully absorbed" rule several DCRs qualify for archiving (DCR-001 most clearly — its four gap categories are all resolved and its ADRs are in sync), but the owner kept all nine active rather than reverse a verdict approved the same day; DCR-003 is now load-bearing regardless, since its amendment holds the sole disposition trail for ~80 Review 0078 findings. Provenance note: all 117 records and 32 OI entries citing a `Source: Review NNNN` now point at the cold store, and no path-link dangles.
* **Update**: `manual/explanation/developer/en/decision-records-and-reviews.md` had the only path-link into the archive (`reviews/reviewed/0081.md` as an OKF `sources:` entry); it now points at `docs/log.md`, and the page explains that the archive was retired and why stable finding ids make that safe.

## 2026-08-07
* **Update**: Ran the `/indy-review-prune` pass over the consolidated record store and the open-issues ledger — 97 live records (58 AD, 30 MADR, 9 DCR) and all 31 OI entries classified with owner approval of the resulting plan. Nothing was deleted and no file moved: in this store "archive" is an `index.yaml` lifecycle value, and record bodies stayed immutable throughout (checked by diffing every touched record against a pre-edit backup — only frontmatter `status:`/`description:`, top-of-body `Status:` lines, and appended amendments changed). Nine records changed lifecycle. Superseded flips: `AD-0011` → AD 0029 (its own 2026-07-20 amendment had recorded the supersession but the status never flipped), `MADR-0006` and `MADR-0008` → AD 0047 (each with a dated amendment; their `have_metadata_digest()` surface has zero occurrences in any commit — accepted-but-never-shipped, the MADR-0007 pattern again), and `MADR-0024` → MADR-0027 (the "only ZIP supports encrypted creation" carve-out was abolished; the rejection site survives with MADR-0027's message). Archived flips: `AD-0030` (nothing named `clear_entries` exists anywhere; ruling discharged) and `MADR-0005` (ruling inverted — the FR-022 policy silently drops link/special entries, exactly the Option 2 it rejected; no single successor record, amendment appended). Migration-artifact corrections: `AD-0023`, `AD-0045`, `AD-0068` were imported from the pre-consolidation `decisions/archive/` directory and mislabelled `superseded` by the 2026-07-23 migration although no successor exists — all three corrected to `archived`, and AD-0068's catalogue title fixed from the colliding pre-renumber "AD 0051:" (frontmatter + `index.yaml` + both indexes; its H1 keeps the original wording, explained by a dated amendment). `index.yaml` now reads 79 active / 14 superseded / 52 archived.
* **Update**: All 31 open-issues entries stay in the active ledger, by owner decision after classification: every RESOLVED entry proved to have a live dependent (source comments citing the id, open successors built on it, reopen triggers, or immutable record bodies pointing at it), so the Status line keeps doing the open/closed work. The owner also clarified that the "resolved entries stay in the ledger" header prose was agent-written on 2026-08-04, not owner policy — relocation is permitted in principle; it simply wasn't worth it for any current entry.
* **Repair**: Knock-on corrections the classification surfaced. `records/README.md` now defines `archived` for all record types (two independent readers had parsed the old wording oppositely). `docs/index.md` mirrored stale frontmatter descriptions for every superseded record with no lifecycle marker — 18 rows refreshed to match the records. `open-issues-resolved.md` OI-0050-006 gained a dated correction note: its Resolution line claims `have_metadata_digest()` was added, which no commit's source ever contained. And the OI-0069-002 residual was mis-identified in two places (`docs/backlog.md` Type 3 and the retired 2026-04-29 plan banner) as R0069-0006, which OI-0069-002's own ledger marks **Landed 2026-04-30** (`Archive::payload_size_for_ratio()`); the true residual is R0069-0063 (`ValidatedSource` construction order, queued for v0.4) — both corrected.

## 2026-08-06
* **Repair**: Worked the documentation-hygiene half of the twenty-three `finding,manual` tickets filed on 2026-08-05. All were triaged against the tree first; ten proved fixable with text-only edits and are now closed, thirteen need a runtime change or an owner ruling and stay open, each annotated with its verdict and tagged `code-behaviour` or `needs-decision`. (The first pass saw only twenty of them: plain `ti list` truncates to terminal rows, so `ti list --open` is the reliable enumeration.) The largest was the DCR-009 residue (`6d74af`): eight documents described the removed `piz` backend, its mmap size cap and the `UNIFIED_ARCHIVE_MAX_MMAP_SIZE` variable as current, and the sweep extended to `dictionary.md`, `walkthroughs.md`, `module-map.md`, `effort-and-risk.md`, `mvp-scope.md`, `rough-schema.md`, `scenario-matrix.md`, eight stale test comments and two test names. Two documents that exist *because* of the pre-collapse tree — `architecture/zip-piz-backend-comparison.md` and the `oi-0065-002` design note — were bannered with a dated outcome rather than rewritten, the latter also flipped to `status: archived`. Also corrected: the UnRAR licence (`1e4408`) — README claimed "free for non-commercial use", the actual clause permits any RAR-handling software free of charge and forbids only building a RAR-compatible archiver, and the licence's own reproduction requirement is now met verbatim in `LICENSE` and in `src/ffi/unrar.rs` comments; the build prerequisites (`ed3647`), which still listed `make` that `build.rs` has not used since the `cc`-crate switch; `CONTRIBUTING.md`'s unrunnable test command and its false "generates FFI bindings" claim (`6221c8`); the manifest-digest overstatements in `STREAM_CRC32.md` (`ca4c10`); the modify-mode caveats in `Limitations.md` and the user manual (`86a73a`); the streaming example that swallowed the error DCR-006 exists to surface (`854caa`); the ledger-composition claim in `open-issues.md` (`d4aa8c`); a rustdoc drift sweep across nine source files plus the crates.io description (`f39387`); and the stale record ids in code comments and one user-visible error message (`58b583`).
* **Repair**: Applied the adversarial verifier's findings before closing anything. It refuted claims the fixers had left standing or introduced: `README.md` credited SFX detection to `goblin`, which is in no manifest and no `use` (classification has been header-field-only since R0079-0010); `STREAM_CRC32.md` still listed TAR as carrying a stored per-entry CRC32, contradicting three other statements in the same file and the libarchive reader, which sets `crc32 = None` unconditionally; `tests/README.md` had a confidently-worded wrong description substituted for a stale filename; `SECURITY.md`'s "public surface (current)" block listed only `ExtractionLimits` while the crate root re-exports `Cap`, `CompressionRatio` and `ExtractionLimitsBuilder`; and `persistence-and-files.md` understated the modification temp-name suffix, which is `.tmp.{pid}.{nanos}.{counter}`. Thirteen more mis-resolving record ids were retargeted in code (`AD 0001`→MADR-0001, `AD 0013`→MADR-0013, `AD 0019`→MADR-0019, `AD 0014`→MADR-0014) while the citations that *are* correct — the UnRAR-mutex `AD 0019` and the deferred-password `AD 0014` — were deliberately left. `build_staging_path`'s rustdoc claimed its unlink/reopen window was "covered" by a record that covers nothing of the sort; it now names R0076-0045, which tracks the race as open.
* **Update**: Landed the text-only halves the triage split out of three still-open decisions, so the deferred tickets carry no doc debt: the byte-ceiling labels said `GB`/`KB` for GiB/KiB values (a ~7.4 % understatement) in `src/security.rs`, `SECURITY.md` and `API_REFERENCE.md`; `ArchiveError::Cancelled`'s rustdoc advertised an `"extract_file"` label that cannot be emitted, and `NotImplemented` did not say that no public call path returns it; `API_REFERENCE.md`'s "Maximum stub size: 50MB" did not mention that detection's 1 MiB window is the effective bound; and the two `reject_unsafe_paths(true)` examples read as copy-pasteable security posture with no hint that enforcement is deferred.
* **Migration**: Brought the last two modules onto the project's file-as-module layout (`aa37c6` item 3): `src/ffi/mod.rs` → `src/ffi.rs` and `src/external/mod.rs` → `src/external.rs`, recorded by git as renames with no content edits, leaving zero `mod.rs` files under `src/`. Module paths are unchanged. Live documents naming the old paths were repointed (`implementation/module-map.md` and two manual pages); the decision records that cite them (DCR-009, AD-0061, MADR-0020) were left alone as immutable history, and `docs/investigation/` picks the change up on its next regeneration. Two misleading examples went with it: `examples/detect_sfx.rs` printed a "Confirmed: Yes/No" line that could only ever print "No" (production detection emits `NotSfx`/`Probable` only), and `examples/stream_checksum.rs` told the user to check the file extension, which `extract_stream_checksum` never consults — it detects from magic bytes.
* **Note**: Four items were surfaced rather than decided. Two are filed as their own tickets: `a095e711` — `LICENSE`'s copyright line still names "7zip-RBinding Contributors", an attribution call only the rights holder can make; and `dd119253` — a record-id ↔ subject check, because an existence check cannot catch this class (every wrong `AD 00NN` in this batch was a real record). Two stay recorded on `aa37c6`: the tracked `.cargo/config.toml` build target-dir pin and the tracked `.unified-archive.toml` temp-dir pin are owner-controlled machine configuration, and the latter is harmless in any case — it is read only by the test harness, behind an env override and a system-temp fallback.

## 2026-08-05
* **Update**: Completed the decision-record integrity amendment batch that the 2026-07-19 design review's §B record-hygiene section had left unexecuted. Twenty-two records now carry a dated, append-only `## Amendment` section: AD-0004, AD-0006, AD-0017, AD-0018, AD-0046, AD-0047, AD-0049, AD-0050, AD-0051, AD-0052, AD-0053, AD-0055, AD-0064, DCR-002, MADR-0007, MADR-0009, MADR-0011, MADR-0012, MADR-0016, MADR-0021, MADR-0022, MADR-0023. Nothing above an amendment was reworded; the only in-place edits were the top-of-body `Status:` line and the frontmatter `status:` / `description:` fields, checked by diffing every file against a pre-edit backup. Each amendment was drafted against the tree and then adversarially re-verified, which changed several conclusions: MADR-0007's "Implemented" claim never shipped in any commit (`git log --all -S` finds no `apply_encryption` / writer-side `with_aes_encryption` in history, and the first `password` in `src/ffi/zip_writer.rs` is the 2026-04-18 rejection), so MADR-0022's "regression" framing rested on a false premise; AD-0050 turned out to be genuinely superseded — AD-0051's own title, Group-A table and Consequences all say "supersede AD 0050" — so it was flipped despite the brief saying otherwise; AD-0017's racy `path.exists()` premise is already atomic `create_new(true)` in both writer constructors; MADR-0023's "unimplemented `open_at_offset`" has been tempfile-backed since DEF-001 closed; AD-0051's out-of-repo plan pointer is recorded as defunct with its in-repo successors named; and MADR-0021's "ZIP has no symlink concept" premise is false while its ruling survives on security-policy grounds. Verification also caught seven citation defects in the draft amendments themselves — a wrong record id (`AD 0020` where the Windows-`panic!` amendment is MADR-0020's), a variant-split overclaim (`Unsupported` pre-dated commit `c9b6685`, and `OperationBlocked` merges two of the four categories), an advisory string attributed to a record that only ever existed in code, three inexact quotations of immutable text, and a miscount — all corrected before landing.
* **Update**: Rewrote `backlog.md` as a closure pass: the Type 1 section is empty (all six items landed on 2026-08-04/05, each recorded under "Closed since the previous run" with its resolution), the Type 2 entries that the amendment batch touched were refreshed (OI-0081-006's record chain is reconciled, so the opt-in is greenfield; OI-0081-002 must derive any `WritableFormat` set from `can_create()` now that three more formats are creatable; the OKF item inherited the 48 legacy `DD-`/`IG-` frontmatter blocks), and a new "Unclassified intake" section parks the twenty `finding,manual` tickets filed on 2026-08-05 — after the scan that produced the classification — so the tracker stays authoritative until the next `/reopen`.
* **Update**: Rewrote `sfx_coverage_report.md` against measured data instead of argued test counts. The 2025-11-13 report claimed the >90 % threshold "requires cargo-tarpaulin or cargo-llvm-cov" and never verified it; `cargo-llvm-cov` 0.8.7 was installed and run over the whole suite, giving `detection.rs` 99.06 %, `result.rs` 100 %, `signatures.rs` 100 %, `stub_types.rs` 98.99 % — about 99.4 % of module lines (crate total 82.43 %). The residual gaps are now named individually: `limits.rs` is consts-only and therefore absent from the report, and `detection.rs`'s Stage-3 `_` fallback arm is unreachable by construction (the signature table holds exactly the seven formats the probe matches) and is documented as an accepted defensive residual. Four targeted tests closed the real gaps (`test_payload_coordinates_populated_and_none`, `test_is_known`, `test_detect_sfx_xz_payload`, `test_detect_sfx_xz_reserved_flag_rejected`), and the test-population figure was corrected: 114 tests live inside `src/sfx/`, while `cargo test --lib sfx` reports 118 because the name filter also matches four facade-level tests in `archive::tests` / `archive::sfx_fallback_error_tests`. All five action items are now ticked.
* **Update**: Added a "The two `status` fields" section to `records/README.md`. An index-consistency audit reported 61 contradictions, of which 57 were an artifact of conflating two different fields: `index.yaml`'s `status:` is the decision lifecycle (`active` / `superseded` / `archived`, and the field a supersession must flip), while a record's own frontmatter `status:` is an OKF documentation-trust marker. The two deliberate divergences are now written down so future audits stop re-raising them — the four OKF-migrated records (`AD-0003`, `AD-0009`, `AD-0014`, `AD-0040`) reading `draft` as a trust reset rather than a lifecycle claim, and the 48 legacy `DD-`/`IG-` files still carrying the v0.1 profile (`timestamp:`, `status: active`) that the owner-gated OKF v0.2 batch owns.
* **Repair**: Reconciled the genuine index contradictions the same audit found. `records/index.yaml` flipped `AD-0007`, `AD-0050`, `MADR-0007`, `MADR-0011`, `MADR-0012`, `MADR-0022` and `MADR-0023` from `active` to `superseded`; `records/index.md` gained the matching `— _superseded_` suffixes; `AD-0007`'s own frontmatter (`status: active`, `description: "Accepted"`) was brought in line with the top-of-body `Status:` line its 2026-07-23 collapse amendment had already rewritten; and the `IG-020-003` row in `index.md` was normalised to `— _superseded (reopened and fixed in Review 0080)_` so the machine-readable marker and the prose coexist. `index.yaml` now carries 13 `superseded` rows against the 6 it showed before the batch, which was the measured cause of reviewers re-litigating settled rulings.

## 2026-08-04
* **Update**: Refreshed the five stale "Partial / Deferred" rows in `project/implementation-slice-checklists.md` against the current tree: RAR creation is implemented behind the `external-rar-create` feature (`src/external/rar.rs`), the libarchive commit path is resolved by design, unified error handling is resolved by the typed `Operation` labels plus the warning-carrying result model (R0068–R0070), and the documentation row is resolved by the API_REFERENCE / USER_MANUAL passes. Only DEF-005's ZIP-modify residual is restated as genuinely open, and the row that still described `piz` as a live ZIP backend was rewritten to name `ZipReader` as the sole one (DCR-009 removed `piz`).
* **Update**: Narrowed `project/open-issues.md` OI-0065-001 to its still-open half with a dated `### Update` block. Innovation I4 (2026-07-22) replaced the `make lib` builder with a `cc`-crate build keyed on `CARGO_CFG_TARGET_OS` and removed the Windows `panic!`, so Required Action 2 is satisfied; what remains open is the libarchive discovery block's host-`cfg` branching, the README half of Required Action 3, and the Windows CI job.
* **Update**: Documented compressed-tar creation across the user-facing surface now that `TarZst` / `TarLz4` / `TarLzma` can be written: `README.md` capability-matrix rows and codec-availability note, `API_REFERENCE.md` enum comments, and `USER_MANUAL.md`, where the three formats moved from the "read/extract only" list into the creatable one.
* **Note**: Retired `superpowers/plans/2026-04-29-deferred-oi-closure.md` (`status: archived`, banner at the top). Every item it tracked has landed except the SFX offset-reuse ratio denominator, which is re-pointed at the OI-0069-002 residual (R0069-0006) so the plan file is no longer a second, drifting tracker.
* **Migration**: Migrated `records/AD-0040-sfx-payload-size-ceiling.md` from the v0.1 profile to the approved v0.2 structure: moved its unchanged legacy timestamp into `generated.at`, recorded `unknown/unknown` as the legacy-producer sentinel, reset `status: active` to `status: draft`, and added the approved `security-safety` category. The document body was unchanged.
* **Update**: Extended `project/taxonomy.md` with the C4 `security-safety` mapping and AD-0040/AD-0015/R0062-0004 traceability tags. The root index already contained the correct C4 target after the earlier 2026-08-04 link repair, so no index rewrite was needed.
* **Execution**: Applied the approved C4 batch at `2026-08-04T23:07:37+09:00` by the current Codex session. The exact model-version identifier was not exposed, so no machine-verification actor was recorded.
* **Repair**: Fixed 89 links in `index.md` whose targets carried a leading `docs/` path segment even though the index itself lives inside `docs/`, so every one resolved to `docs/docs/…`. All 89 now resolve. No link target changed identity — only the erroneous prefix was removed. Fixed the one remaining instance of the same defect, in `project/open-issues.md`.
* **Update**: Added the 59 documents that existed under `docs/` but were absent from `index.md`: `project/open-issues-resolved.md`, `records/README.md`, `records/MADR-0030`, `records/DCR-006`–`DCR-009`, the three `architecture/` design-review and backend-comparison notes, `backlog.md`, and the 48 migrated `DD-`/`IG-` records. `index.md` is once again a complete file-by-file listing.
* **Migration**: Split the retired `records/legacy/{Decisions,Ignores}.md` write-path logs into one record file per entry — 24 `DD-NNN-NNN` and 24 `IG-NNN-NNN` records — so every `records/index.yaml` id addresses a real file. Bodies were preserved verbatim (verified by round-trip diff); only section-heading depth changed (`###` → `##`) now that each record is a standalone document. Repointed all 48 `file:` rows in `index.yaml`, added 48 `legacy_log_split:` id rows plus 2 path redirects to `redirects.yaml`, and listed every record in `records/index.md`.
* **Migration**: Relocated the 48-entry resolved-issue archive from `records/legacy/Open_Issues_Resolved.md` to `project/open-issues-resolved.md`. Resolved-issue history is project tracking, not a decision record, so it now sits beside the active ledger. Entry bodies unchanged. Repaired the dangling `reviews/Open_Issues_Resolved.md` pointer in `project/open-issues.md`, whose entry count was also wrong (claimed 49, actual 48).
* **Update**: Folded the two retired logs' governance prose — the DD and IG inclusion criteria and the DD-over-IG precedence rule — into `records/README.md` so it did not die with the monoliths. Recorded `IG-020-003` as `superseded` in `index.yaml`: its REJECT was formally reversed by the Review 0080 gate (R0080-0001/0002).
* **Update**: Repointed four `reviews/Ignores.md` citations in `records/AD-0036`, `AD-0045`, and `AD-0046` at the new per-record files. These referenced a file that no longer exists, which AD-0023 explicitly exempts from its archival-immutability rule ("unless the archived content is actually wrong"), and matches the "IDs/paths only" change the 2026-07-23 consolidation already made.
* **Migration**: Replaced the monolithic `architecture/{architecture,codebase}_investigation.md` with the sharded `investigation/` snapshot: a hub `README.md`, 24 per-module shards under `investigation/codebase/`, and 7 per-concern files under `investigation/architecture/`. Prose that still matched the code was carried over verbatim; stale claims were re-analysed against `src/` at this revision, most consequentially the DCR-009 removal of the `piz` ZIP backend, which the legacy snapshot still documented as a live dual-backend strategy. The legacy `DD-001`–`DD-016` architecture decisions were carried across without renumbering; 23 new ones were appended from `DD-017`. Removed the two legacy files and their two now-orphaned `.ko.md` siblings, with the matching `.translate/registry.yaml` rows.
* **Note**: Per the investigation skills, `investigation/` carries OKF-style frontmatter but is **not** part of this bundle and is never listed in `index.md`. It is a regenerable snapshot and is gitignored — `.gitignore` now ignores `docs/investigation/` in place of the two retired filename entries.

## 2026-08-02
* **Migration**: Migrated `records/AD-0014-reject-deferred-password-validation.md` from the v0.1 profile to the approved v0.2 structure: moved its unchanged legacy timestamp into `generated.at`, recorded `unknown/unknown` as the legacy-producer sentinel, reset `status: active` to `status: draft`, and added the approved `security-safety` category. The document body was unchanged.
* **Update**: Extended `project/taxonomy.md` with the C3 `security-safety` mapping and Review 0025 traceability tags; corrected the C3 root-index link after the existing `records/` consolidation.
* **Execution**: Applied the approved C3 batch at `2026-08-02T20:39:31+09:00` by the current Codex session. The exact model-version identifier was not exposed, so no machine-verification actor was recorded.
* **Migration**: Migrated `records/AD-0009-isolate-unsafe-behind-safe-wrappers.md` from the v0.1 profile to the approved v0.2 structure: moved its unchanged legacy timestamp into `generated.at`, recorded `unknown/unknown` as the legacy-producer sentinel, reset `status: active` to `status: draft`, and added the approved `ffi-safety` category. The document body was unchanged.
* **Update**: Extended `project/taxonomy.md` with the approved `ffi-safety` definition, C2 traceability mapping, and governed-concept link; corrected the C2 root-index link after the existing `records/` consolidation.
* **Execution**: Applied the approved C2 batch at `2026-08-02T20:08:36+09:00` by the current Codex session. The exact model-version identifier was not exposed, so no machine-verification actor was recorded.
* **Migration**: Migrated `records/AD-0003-safety-gates-before-extraction.md` from the v0.1 profile to the approved v0.2 structure: moved its unchanged legacy timestamp into `generated.at`, recorded `unknown/unknown` as the legacy-producer sentinel, reset `status: active` to `status: draft`, and added the approved `security-safety` category. The document body was unchanged.
* **Creation**: Added `project/taxonomy.md`, distilled from approved legacy bundle vocabulary. Its `unknown/unknown` producer is the migration-derived sentinel, not verification evidence.
* **Update**: Added the taxonomy concept to project indexes and corrected the C1 root-index link after the existing `records/` consolidation.
* **Execution**: Applied this approved batch at `2026-08-02T19:31:02+09:00` by the current Codex session. The exact model-version identifier was not exposed, so no machine-verification actor was recorded.

## 2026-07-17
* **Migration**: Revalidated docs/ against OKF v0.1 — added frontmatter to 0 docs, repaired 0, merged 0, regenerated index.md and 7 sub-indexes.

## 2026-07-06
* **Migration**: Brought docs/ to OKF v0.1 — prepended frontmatter to 130 docs (incl. legacy index.md renamed to project-index.md), regenerated index.md and 7 sub-indexes.
