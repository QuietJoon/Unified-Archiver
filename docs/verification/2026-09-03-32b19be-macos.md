# Release gate — 2026-09-03 (macOS dev host)

First record under AD-0070. Produced by hand rather than by
`scripts/release-gate.sh`, because the script landed in this same change and
its contention guard correctly refused to start while foreign cargo runs were
live; the lanes below are the ones it defines, run individually with their own
exit statuses captured.

## Fingerprint

- commit: `32b19be` **plus the uncommitted change this record is part of** — so
  this is evidence for that tree, not for `32b19be` alone. The next record
  should be taken from a clean tree by the script.
- host: `Darwin 25.6.0 arm64` / macOS 26.6.2
- rustc: `rustc 1.98.0 (88d9e12ae 2026-08-18)`
- cargo: `cargo 1.98.0 (797e8a9bc 2026-08-05)`
- target dir: `/Volumes/Scratch/cargo_target/ua` (owner set `CARGO_TARGET_DIR` during this session; never set by any script here)
- TMPDIR: `/Volumes/Temp/claude` — 3.8 GiB volume, 1.9Gi free

## Lanes

| lane | command | exit | result |
|---|---|---|---|
| fmt | `cargo fmt --all -- --check` | 0 | — |
| clippy | `cargo clippy --all-targets --all-features -- -D warnings` | 0 | zero Rust warnings |
| clippy-minimal | `cargo clippy --no-default-features --all-targets -- -D warnings` | 0 | zero warnings — **first time this profile has ever been clean** |
| test-all-features | `cargo test --all-features -- --test-threads=4` | 0 | 41 suites, 1974 passed, 0 failed, 12 ignored |
| test-no-default-features | `cargo test --no-default-features -- --test-threads=4` | 0 | 41 suites, 1745 passed, 0 failed, 10 ignored — **first recorded run of this profile** |

## Not run

- `check-windows-cfg` — not run. Owner-invoked only (2026-09-01 hold).
- `check-linux-musl` — not run. OI-0080-001 parked pre-v2.
- **Windows test run — not runnable on this host.** Needs the owner's Windows
  machine; pending as of this record.
- **Linux test run — not runnable on this host.** OrbStack is installed and
  deferred by owner ruling 2026-09-03.

## What this run found

The `--no-default-features` profile had never been built. It compiles and
passes, but eight test files carried imports and helper functions that dangle
when `rar-support` is off, which `-D warnings` turns into build failures:
`performance_test`, `unified_api_test`, `contract/progress_contract`,
`password_handling_test`, `property_tests`, `crc32_verification_test`,
`extraction_test` and `integration/format_compatibility`. All are now
feature-gated. This is the whole argument for the lane existing: the defect was
invisible to every gate the project had been running.
